services:
  mongo:
    image: mongo:7
    container_name: giffy-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - giffy-mongo-data:/data/db

volumes:
  giffy-mongo-data:
